# VisualizationSet

-- 刚开始一直用json4u网站的json可视化, 结果现在关闭了。引导到同功能的treease.com网站, 不过是要钱的, 好吧, 我不用了！

## JSON解析
- 结构视图-table: json中单数据(元素数量大适用一点？), 改到后面不怎么用了。后面可能就删掉了。
- 结构视图-graph: 主用于展示父节点与子节点的关联关系。

## SQL解析
- php字符串: 为了烦人的php接口响应的sql字符串, 每次直接copy后, 还要自己替换双引号与转义、制表符....就加上了, 实际就是正则组合集。
- Hibernate原生: 目前我已经用不到了, 属实是无聊才加的功能。